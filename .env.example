## .env.example - environment variables for BLT-Leaf
# Copy this file to .env (the setup script will do this and overwrite .env)
# Fill values or leave blank for defaults. Do NOT commit your real secrets.

# D1 database id
D1_DATABASE_ID=

# GitHub OAuth App credentials (required for per-user authentication)
# Create an OAuth App at: https://github.com/settings/developers
# Set callback URL to: https://your-worker.workers.dev/api/auth/github/callback
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# Session encryption secret (generate a random 32+ character string)
# Used to encrypt session tokens and OAuth tokens at rest
SESSION_SECRET=

# Optional: GitHub personal access token (fallback for unauthenticated requests)
# This is now optional as users will authenticate via OAuth
GITHUB_TOKEN=

# Cloudflare account ID (optional, used by some workflows)
CLOUDFLARE_ACCOUNT_ID=

# Optional: API token for Wrangler if you prefer non-interactive login
WRANGLER_API_TOKEN=
